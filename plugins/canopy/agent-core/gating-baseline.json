{
  "_doc": "Fleet-baseline deny rails, keyed by channel mount — the centralized half of the gating engine (agent-core design, 2026-07-13, chunk B). Shipped with the canopy plugin so a rail fix propagates to every agent via /canopy:update. Each agent's hooks/gating_guard.py reads its config/gating.json `channels` list, pulls these rails for the mounted channels, substitutes {slug}, and merges them IN FRONT of the repo's own `deny` list at call time. ADD-ONLY: an agent's local config can add rails; it can never remove a baseline rail. Rails here must stay channel-generic — agent-specific rails belong in that agent's config/gating.json.",
  "channels": {
    "email": [
      {
        "tool": "Bash",
        "pattern": "(?:^|[\\n;&|(])\\s*gog\\s+gmail\\s+(send|reply)\\b",
        "message": "BLOCKED: raw `gog gmail send/reply` bypasses {slug}'s HTML wrapper and thread_id capture. Send via bin/{slug}-email (the shared canopy email engine) instead — write the body to a file and pass --body-file."
      },
      {
        "tool": "Bash",
        "pattern": "canopy\\s+email\\s+send\\b(?=[^\\n]*--account)",
        "message": "BLOCKED: `canopy email send --account` overrides {slug}'s repo identity — one mailbox per agent, never shared (identity bleed is the fleet's one hard rule). Send via bin/{slug}-email, which pins this repo's identity."
      }
    ],
    "gws": [
      {
        "tool": "Bash",
        "_pattern_doc": "Deliverable filing rail for the RAW gog path (agent-core/deliverables.md rule 1). BROADENED 2026-08-13: this used to fire only on `gog drive upload --convert`, and each agent hand-added its own extra verbs locally — so the enforced set was a short, per-agent list of the creation commands somebody had thought of. `gog sheets create` was in nobody's list, and on 2026-08-12 Eva built a 45-row target roster with it straight into her My Drive root, unshared. A denylist of verbs always trails the tool surface, so this now matches the SHAPE — any gog subcommand that creates a Drive object — and requires a destination. `--help` is exempted so the rail can't stop you reading the command's own usage.",
        "pattern": "(?:^|[\\n;&|(])\\s*gog\\b[^;&|\\n]*\\b(?:(?:docs|sheets|slides|forms)\\s+(?:create|new)|drive\\s+(?:mkdir|upload))\\b(?![^;&|\\n]*(?:--parent|--help|(?:^|\\s)-h(?:\\s|$)))",
        "message": "BLOCKED: {slug} never creates a Google Doc / Sheet / Slide / folder in My Drive root — it is invisible to the team, a dead link to @dimagi.com recipients, and it dies with the agent identity. Pass `--parent <folderId>` under {slug}'s OWN root. Resolve the destination instead of pasting an id: `canopy gdoc publish --project \"<Project>\"` (markdown → Doc) or `canopy gsheet publish --project \"<Project>\"` (TSV/CSV → Sheet) both find-or-create <root>/Projects/<Project>, file, share and verify in one call; `--area \"Process State\"` for a durable tracker. SEARCH FIRST — reuse the project's existing folder rather than spawning a parallel one. See agent-core/deliverables.md."
      },
      {
        "tool": "Bash",
        "_pattern_doc": "Same rail for the canopy publishing engines. Without a destination flag `publish` falls back to the agent's Drive ROOT, i.e. beside Projects/ rather than inside it — the mistake deliverables.md rule 1 names explicitly. `--replace` is exempt (it keeps the existing doc's home) and so is `--help`: before 2026-08-13 this rail matched `canopy gdoc publish --help`, so an agent could not read the usage of the command it was being told to use.",
        "pattern": "(?:^|[\\n;&|(])\\s*canopy\\s+(?:gdoc|gsheet)\\s+publish\\b(?![\\s\\S]*(?:--parent|--replace|--project|--area|--help))",
        "message": "BLOCKED: `canopy gdoc/gsheet publish` with no destination files into {slug}'s Drive ROOT, beside Projects/ instead of inside it. Pass `--project \"<Project>\"` (a per-project subfolder, find-or-create, reused across turns), `--area \"Process State\"` for a durable tracker, or `--replace <id>` to update an existing artifact in place. See agent-core/deliverables.md."
      },
      {
        "tool_pattern": "^mcp__.*(?:gdrive|gws|drive)__(?:drive_create_file|drive_create_folder|drive_create_doc_from_markdown|drive_copy_file|docs_create|docs_copy_template|sheets_create(?!_tab))",
        "_pattern_doc": "The MCP half of the same rail, added 2026-08-13. Every rail above is `tool: Bash`, so an agent holding a Drive-creating MCP tool (chrome-sales gdrive, ace-gdrive) could file anywhere with nothing to stop it — the Bash path was railed while the richer path beside it was wide open. Matched on tool NAME shape via `tool_pattern`, because the same creator carries a different plugin mount per agent. The pattern then tests the tool's ARGUMENTS (the hook JSON-serializes MCP input) for any of the parent/folder keys these servers use; absent all of them, the object lands in My Drive root. `sheets_create` is negative-lookahead'd against `_tab` on purpose: `sheets_create_tab` adds a worksheet INSIDE an existing spreadsheet, creates no Drive object, and takes a spreadsheetId rather than a parent — so the rail had no satisfiable form and simply blocked a legitimate operation forever. Found 2026-08-26, when an agent wanting a Notes tab on a sheet it already owned was told to pass a parent argument the tool does not have, and worked around it by writing into spare columns instead.",
        "pattern": "^(?![\\s\\S]*\"(?:parent|parent_id|parentId|parent_folder_id|folder_id|folderId|parents)\"\\s*:\\s*\"?[^\"null,}\\s])",
        "message": "BLOCKED: that MCP call creates a Drive object with no destination, which lands it in {slug}'s My Drive root — invisible to the team and dead-linked to @dimagi.com recipients. Pass the server's parent/folder argument, pointing at a per-project subfolder under {slug}'s OWN root. Easier: `canopy gdoc publish --project \"<Project>\"` / `canopy gsheet publish --project \"<Project>\"` resolve, file, share and verify in one call. See agent-core/deliverables.md."
      }
    ]
  }
}
