# canopy issue triage — 2026-09-14

**Scope contract:** triage + quick wins · **Code ref:** `efa23d6` · **21 open issues, 1 open PR**

Nothing carried forward. The prior sidecar (2026-07-29) covered 4 issues; 220 commits
and 259 changed files have landed since, so every recorded `evidence_paths` entry had
moved and all 21 were re-triaged fresh.

## Tier counts

| tier | count |
|---|---|
| blocks-e2e | 3 |
| harness | 12 |
| polish | 6 |

## Clusters

- **verdict-contract-at-emit** (#547, #548) — one emit-time judge-output validator.
  #547 tightens `fix_kind` / `verdict` schema conformance and gives the gate a home;
  #548 hangs per-pass provenance assertions off it. **Sequence #547 first.**
- **loop-lens-false-gates** (#621, #624, #625) — deterministic DDD lenses emitting a
  gate on an input they were never told changed.
- **cross-iteration-provenance** (#556, #625) — stamp per-iteration render provenance so
  a score series says which artifact it measured.
- **scene-narrative-list-form** (#623) — SHIPPED, PR #642.

No duplicates found: no issue in this set is a restatement of another.

## `fix-and-unwind` candidates — BOTH bugs still live, so BOTH workarounds stay

Two open canopy issues are cited as workarounds in **hal's** prose (the cross-repo split
that makes these invisible to a single-repo triage). Neither was unwound, because
deleting a workaround whose bug is still live is strictly worse than leaving both:

| issue | workaround | why it stays |
|---|---|---|
| #416 | `hal/skills/ace-review/SKILL.md:144,401` | only the **calibration** half shipped (`a2006bd`); the lifecycle-shaped taxonomy that the issue's title names has zero implementation |
| #615 | `hal/skills/turn/SKILL.md:446-462` | preflight still compares only the PLUGIN sha; the version-label checks are blind (both sides read `0.2.476` in the reported failure) |

#416 is the standing reminder that a code citation is a strong prior and never a verdict —
its tests and fix commit exist, and the headline ask does not.

## Open PR

**#639** — `blocked`, and **not mine**: a live sibling hal turn owns it and is parked at its
approval gate. Its red `pytest` had nothing to do with its diff — `greenlet` 3.5.6 published
at 14:22:33Z as a **partial release** (3 files, no sdist, no Linux wheels), and canopy commits
no `uv.lock`, so `uv sync --extra browser` broke on **every** canopy PR. Cleared at 15:42:51Z
(79 files, sdist, 56 linux wheels). Reported; re-running and merging is the owner's call.

## Acted this run

- **#623 → shipped** as PR #642, merged 21:12:38Z. Five call sites, not the two reported.
- **#552 → closed** (completed). Fixed in `3289877`, verified an ancestor of `origin/main`;
  prose corrected AND backed by the `Skill`-tool deny rail in `gating-baseline.json`.
- **#627 → shipped** (this PR).

## Handed back

18 `implement` issues. Highest-value next targets, in tier order:

1. **#546** (blocks-e2e, M) — preflight still fires the payoff action before filming.
2. **#568** (blocks-e2e, M) — `canopy gdoc` drops every hyperlink and reports clean.
   Half is locally verifiable; the repair needs a live Docs round-trip.
3. **#547 → #548** (harness, S then M) — the verdict-contract cluster, in that order.
4. **#624, #625, #621** (harness/polish, all S) — the loop-lens cluster.
5. **#614** (harness, S) — **caution:** `extra="forbid"` on `Scene` may break sibling
   (ACE) specs carrying stray keys; prefer an explicit unknown-key check in the validator.

**#618 is preventive, not an incident** — two independent greps over tracked files found
**zero** live share tokens, and new run dirs already moved outside the repo.
